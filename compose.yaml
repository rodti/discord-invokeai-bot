services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      INVOKEAI_URL: ${INVOKEAI_URL:-http://host.docker.internal:9090}
      BOT_CONFIG: ${BOT_CONFIG:-/app/config.json}
    volumes:
      - ./config.json:/app/config.json:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
